# Runtime + dev deps. Ranges (not == pins) so installs work on Python 3.11-3.14
# and on Windows, where old exact pins (e.g. numpy==2.2.6, psycopg2-binary==2.9.10)
# have no wheels and fail to build. Docker (Linux) still resolves to recent versions.
# Note: tests run on SQLite (see tests/conftest.py); asyncpg/psycopg2-binary are only
# needed for the Postgres runtime path.
fastapi>=0.115.12,<1
uvicorn[standard]>=0.34.2,<1
sqlalchemy[asyncio]>=2.0.41,<3
asyncpg>=0.30.0,<1
alembic>=1.15.2,<2
python-jose[cryptography]>=3.4.0,<4
passlib[argon2]>=1.7.4,<2
python-multipart>=0.0.20,<1
redis>=5.3.0,<6
celery[redis]>=5.5.2,<6
psycopg2-binary>=2.9.10,<3
pydantic-settings>=2.9.1,<3
numpy>=1.26,<3
scipy>=1.12,<2
# dev
pytest>=8.4.1,<10
httpx>=0.28.1,<1
pytest-asyncio>=0.26.0,<1
testcontainers[postgres]>=4.9.2,<5
aiosqlite>=0.21.0,<1
email-validator>=2.2.0,<3
limits>=5.8.0,<6
